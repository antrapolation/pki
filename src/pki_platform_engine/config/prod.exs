import Config

# Production configuration is handled via runtime.exs in each portal.
# The platform engine is a dependency — its repos are configured by the
# portal that includes it (platform_portal, ca_portal, ra_portal).
