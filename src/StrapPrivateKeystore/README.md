# StrapPrivateKeystore

Strap Private Keystore Protocol

Purpose to standardize the API between soft and secmod keystore for module building on top of the private keystore

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `strap_private_keystore` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:strap_private_keystore, "~> 0.1.0"}
  ]
end
```

Documentation can be generated with [ExDoc](https://github.com/elixir-lang/ex_doc)
and published on [HexDocs](https://hexdocs.pm). Once published, the docs can
be found at <https://hexdocs.pm/strap_private_keystore>.
