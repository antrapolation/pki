defmodule PkiValidation do
  @moduledoc """
  PKI Validation Service — OCSP responder, CRL publisher, and LDAP directory.

  This service reads certificate status data and serves validation responses.
  It has no signing keys and is read-only and horizontally scalable.
  """
end
