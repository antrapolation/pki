# SoftHSM2 config for CA engine (client side)
# Token directory is mounted from the softhsm2 container via shared volume
directories.tokendir = /hsm/tokens
objectstore.backend = file
log.level = INFO
